package com.sascom.chickenstock.portfolio.domain; import jakarta.persistence.*; import lombok.*; import java.time.Instant; @Entity @Table(name="fill_record", uniqueConstraints=@UniqueConstraint(columnNames={"orderId","accountId"})) @Getter @Setter @NoArgsConstructor @AllArgsConstructor public class FillRecord { @Id @GeneratedValue(strategy=GenerationType.IDENTITY) private Long id; private Long orderId; private Long accountId; private Long companyId; private String side; private Long price; private Long quantity; private Instant appliedAt = Instant.now(); }